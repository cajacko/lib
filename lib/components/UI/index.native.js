// @flow

import { Text, View, FlatList, TextInput, SectionList } from 'react-native';
import Button from './Button.native';

export { Left, Right, ListItem, Header, Body, Icon } from 'native-base';
export { FlatList, Button, TextInput, SectionList };
export const P = View;
export const Div = View;
export const Span = Text;
